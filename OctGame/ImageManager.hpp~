#include <opencv2/opencv.hpp>
#include <vector>
#include <string>

using namespace std;

class ImageManager {
public:
    int loadImage(string filepath, bool isBmp=false);
    int loadImage(string filepath, bool isBmp=false, int width, int height);
    cv::Mat* getImage(int handle);
private:
    vector<cv::Mat> imageList;
};
